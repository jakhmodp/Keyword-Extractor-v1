# For Spacy model
python -m spacy download en_core_web_lg
python -m spacy download en_core_web_sm
python -m spacy download en

# Pip upgrade
python.exe -m pip install --upgrade pip

# Wordcloud gives wheel issue with 3.8 and above versions when using Virtual environemnt. So use 'conda environment'
# On Heroku, pywin32==304 version had issue, removed the library from requirements.txt to resolve the issue.


